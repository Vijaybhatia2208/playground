# Stale Closures in JS

- A stale closures in javascript happen when function rememeber old value of variable , event after variable is changed.

A closure is when
Rememebering is called closure

when you create a function javascript it is remembers vairables and functions in it's innner scope, even those vairable are not in it's scope they are declared and used outside

## Closure is when function remembers it's vairable's from it's outer scope even the function is finished executing executed
